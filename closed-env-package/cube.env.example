# ============================================================
# Cube.js — переменные окружения
# Скопируйте в .env в корень вашего Cube-проекта
# ============================================================

# === ВАРИАНТ 1: DuckDB (рекомендуется для закрытого контура) ===
CUBEJS_DB_TYPE=duckdb
CUBEJS_DB_DUCKDB_DATABASE_PATH=/path/to/data.duckdb

# === ВАРИАНТ 2: PostgreSQL / GreenPlum ===
# CUBEJS_DB_TYPE=postgres
# CUBEJS_DB_HOST=localhost
# CUBEJS_DB_PORT=5432
# CUBEJS_DB_NAME=your_database
# CUBEJS_DB_USER=your_user
# CUBEJS_DB_PASS=your_password
# CUBEJS_DB_SSL=false

# === Общие настройки ===
CUBEJS_DEV_MODE=true
CUBEJS_API_SECRET=your-secret-key-here
CUBEJS_CACHE_AND_QUEUE_DRIVER=memory
PORT=4000

# === Для DuckDB — установите драйвер ===
# npm install @cubejs-backend/duckdb-driver
